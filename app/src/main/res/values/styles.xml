<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Theme pour le Login -->
    <style name="AppTheme.Login" parent="android:Theme.Holo.Light">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">#D86060</item>
        <item name="android:buttonStyle">@style/AppTheme.Login.Button</item>
        <item name="android:editTextStyle">@style/AppTheme.Login.TextField</item>
    </style>

    <style name="AppTheme.Login.Button" parent="android:style/Widget.Button">
        <item name="android:background">@drawable/rounded_button</item>
        <item name="android:textColor">#FFF</item>
        <item name="android:padding">10dp</item>
    </style>

    <style name="AppTheme.Login.TextField" parent="android:style/Widget.EditText">
        <item name="android:background">@drawable/rounded_edittext</item>
        <item name="android:padding">10dp</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_marginBottom">50dp</item>
        <item name="android:textColor">#8F0E0E</item>
    </style>

    <!-- Theme pour l'activité de la map -->
    <style name="AppTheme.Map" parent="android:Theme.Holo.Light">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:spinnerStyle">@style/AppTheme.Map.TagsSpinner</item>
    </style>

    <style name="AppTheme.Map.TagsSpinner" parent="android:style/Widget.Spinner">
        <item name="android:background">@drawable/rounded_spinner</item>
        <item name="android:spinnerMode">dropdown</item>
    </style>

</resources>
